<Page x:Class="WpfApp1.HIPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="800"
      Title="HIPage" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель поиска и фильтрации -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="tbSearch" Width="200" Height="30" TextChanged="tbSearch_TextChanged" Margin="0,0,20,0"/>

            <TextBlock Text="Фильтр по локации:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="cbLocationFilter" Width="150" Height="30" SelectionChanged="cbLocationFilter_SelectionChanged"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="DGridHi" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название кластера" Binding="{Binding cluster_name}" Width="*"/>
                <DataGridTextColumn Header="Локация" Binding="{Binding location}" Width="200"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding description}" Width="150"/>
                <DataGridTemplateColumn Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" Click="BtnEdit_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Панель кнопок -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <Button x:Name="BtnAdd" Content="Добавить" Width="100" Height="35" Margin="0,0,10,0" Click="BtnAdd_Click"/>
            <Button x:Name="BtnDelete" Content="Удалить" Width="100" Height="35" Click="BtnDelete_Click"/>
        </StackPanel>

    </Grid>
</Page>