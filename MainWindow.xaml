<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Кластеры ТУРСУНТ" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <TextBlock Text="Кластеры ТУРСУНТ" FontSize="20" FontWeight="Bold" 
                       Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <!-- Верхняя панель с пользователем и кнопками -->
        <Border Grid.Row="1" Background="#34495E" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="tbUserInfo" Text="Пользователь: " Foreground="White" 
                           VerticalAlignment="Center" Margin="0,0,15,0"/>
                <Button x:Name="btnLogout" Content="Выйти" Width="80" Height="30" 
                        Click="btnLogout_Click" Background="#E74C3C" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Frame для навигации -->
        <Frame x:Name="MainFrame" Grid.Row="2" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>