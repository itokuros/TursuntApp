<Window x:Class="WpfApp1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Автор" FontSize="14" Foreground="Gray" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Вход в систему" FontSize="22" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="2" Text="Логин:" FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="tbLogin" Grid.Row="3" Height="35" FontSize="14" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="4" Text="Пароль:" FontSize="14" Margin="0,0,0,5"/>
        <PasswordBox x:Name="pbPassword" Grid.Row="5" Height="35" FontSize="14" Margin="0,0,0,20"/>

        <!-- Кнопка Войти -->
        <Button x:Name="btnLogin" Grid.Row="6" Content="Войти" Height="40" FontSize="14"
                Background="#0077BE" Foreground="White" Click="btnLogin_Click" Margin="0,0,0,10"/>

        <!-- Кнопка Регистрация (опционально) -->
        <Button x:Name="btnRegister" Grid.Row="7" Content="Регистрация" Height="35" FontSize="12"
                Background="Transparent" Foreground="#0077BE" BorderThickness="0"
                Cursor="Hand" Click="btnRegister_Click" Visibility="Collapsed"/>

        <TextBlock x:Name="tbError" Grid.Row="8" Text="" Foreground="Red" 
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>